name: practice pipeline on gihub actions

on:
  push:
    branches: main
jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: print a message
        run: echo "Pipeline chal rahi hai"

      - name: Show current directory
        run: ls -la
    
